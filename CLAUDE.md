# demo.woojeongalex

## 프로젝트 개요
"영상자료 디지털화사업 개발물 DB 영문화작업 업체 선정" 제안요청서(출처: 한국소프트웨어진흥원)를 기반으로,
개발 진행 보고서를 Jekyll 정적 사이트로 문서화하는 프로젝트.

- 사업명: (가칭) 영상자료 디지털화사업 개발물 DB 영문화작업 지원 시스템 — **임시 이름, 확정 시 이 문서와 표지/설정을 함께 갱신할 것**
- 개발자: woojeongalex
- 개발기간: 2026.08.20 ~ 2026.10.27

## 디렉터리 구조
- `/` — 리포 루트, 이 CLAUDE.md와 최상위 README만 위치
- `demo.woojeongalex.cloud/` — Jekyll 사이트 루트 (실제 사이트 소스는 전부 이 안에 있음)

## 개발 환경
- Ruby는 시스템 패키지가 아닌 **rbenv**로 관리 (`~/.rbenv`, `~/.bashrc`에 `rbenv init` 등록됨)
- Ruby 버전: 3.3.12 (rbenv global)
- Jekyll 4.4.1 / Bundler 4.0.19 (gem install, rbenv 환경 하에 설치됨)
- 새 셸에서 ruby/jekyll/bundle 명령을 쓰려면 `~/.bashrc`가 로드되어 있어야 함 (`eval "$(rbenv init - bash)"`가 이미 등록되어 있음). 비대화형 셸에서는 필요 시 직접
  `export PATH="$HOME/.rbenv/bin:$PATH" && eval "$(rbenv init - bash)"` 후 명령 실행.

## 실행 규칙
- 로컬 미리보기 서버: `demo.woojeongalex.cloud/` 안에서
  `bundle exec jekyll serve --host 0.0.0.0 --port 4000`
  - `--host 0.0.0.0` 필수 (기본값 localhost 바인딩이면 WSL 밖에서 접근 불가)
  - WSL2 환경에서는 Windows 쪽 브라우저에서 `http://localhost:4000` 으로 바로 접속 가능 (자동 포트포워딩)
- `_config.yml`은 서버 실행 중 변경해도 자동 반영되지 않음 → 변경 시 서버 재시작 필요
- 실제 배포는 GitHub Pages가 담당. 서버에서 띄우는 `jekyll serve`는 어디까지나 로컬 미리보기 용도이며, 별도 배포 절차는 아직 정해지지 않음.

## 문서 구조 규칙 (Jekyll 페이지 순서)
1. **표지 (1페이지)**: 사업명 / 개발자 / 개발기간 / 문서버전
2. **목차 (2페이지)**: 아래 4단계 구조를 따름
   1. 사업 개요 (사업 목적 / 주요 사업 내용 / 기대 효과)
   2. 개발 요구사항 (목적 / 개발 범위 / 참여조건 / 선정·평가 기준 / 영문화 작업 내용 / 평가 방법 / 진행 절차)
   3. 개발 수행 지침 (일반 사항 / 신청·진행 방법 및 절차)
   4. 산출물 작성 요령 및 서식
3. 이후 각 목차 항목이 본문 페이지로 이어짐
- 문서 전체는 한국어로 작성
- 원본 제안요청서 목차의 "제안요청내용/제안서" 표현은 "개발요구사항/산출물"로 바꿔서 사용 (제안서 작성 관점이 아닌 실제 개발 문서 관점이므로)

## 진행 상황
- 2026-08-20: 개발 환경 구축 완료 — rbenv 설치, Ruby 3.3.12 빌드, Jekyll·Bundler 설치
- 2026-08-20: `demo.woojeongalex.cloud/`에 Jekyll 기본 사이트(minima 테마) 생성, `bundle install` 완료
- 2026-08-20: 로컬 미리보기 서버(`0.0.0.0:4000`) 기동 확인
- 2026-08-20: 표지(1페이지)·목차(2페이지) 텍스트 초안 작성 (대화 중 전달, 아직 실제 `.md` 파일로는 미반영)

## TODO
- [ ] 표지/목차 초안을 실제 Jekyll 페이지 파일로 반영 (예: `index.markdown` 또는 별도 `_pages/`)
- [ ] `_config.yml`의 `title`, `email`, `description`, `url` 등을 기본값에서 실제 값으로 교체
- [ ] 사업명 확정 후 표지·설정 전체에 일괄 반영
- [ ] 목차 각 항목(사업 개요 ~ 산출물 서식)의 본문 콘텐츠 작성
- [ ] 배포 방식(GitHub Pages 등) 확정 및 절차 문서화
